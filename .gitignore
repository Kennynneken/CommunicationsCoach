# Raw media can be large — commit only if you want history of takes
sessions/*.mp4
sessions/*.mov
sessions/*.m4a
sessions/*.wav
sessions/*.mp3
!sessions/.gitkeep
# Intermediate audio
analysis/**/audio.wav
__pycache__/
*.pyc
# MediaPipe task models — fetched by setup.sh
pipeline/models/*.task
